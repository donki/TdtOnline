<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <ImageView
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:src="@drawable/ic_settings"
                android:contentDescription="@null" />

            <TextView
                android:id="@+id/title"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="12dp"
                android:textSize="22sp"
                android:textStyle="bold"
                android:textColor="@color/text_primary" />
        </LinearLayout>

        <!-- Listas de canales -->
        <LinearLayout style="@style/AboutCard">
            <TextView android:id="@+id/lists_title" style="@style/AboutCardTitle" />
            <TextView android:id="@+id/lists_hint" style="@style/AboutHint" />

            <!-- Una fila por lista: direccion y papelera. -->
            <LinearLayout
                android:id="@+id/lists"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="10dp"
                android:orientation="vertical" />

            <!-- Nueva lista: direccion y «+». -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="10dp"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <EditText
                    android:id="@+id/new_url"
                    android:layout_width="0dp"
                    android:layout_height="44dp"
                    android:layout_weight="1"
                    android:paddingStart="14dp"
                    android:paddingEnd="14dp"
                    android:background="@drawable/chip_background"
                    android:textSize="14sp"
                    android:textColor="@color/text_primary"
                    android:textColorHint="@color/text_secondary"
                    android:inputType="textUri"
                    android:imeOptions="actionDone|flagNoExtractUi"
                    android:maxLines="1"
                    android:importantForAutofill="no" />

                <ImageButton
                    android:id="@+id/btn_add"
                    android:layout_width="44dp"
                    android:layout_height="44dp"
                    android:layout_marginStart="8dp"
                    android:padding="10dp"
                    android:scaleType="fitCenter"
                    android:background="@drawable/chip_background"
                    android:src="@drawable/ic_add"
                    android:focusable="true"
                    android:clickable="true"
                    android:contentDescription="@null" />
            </LinearLayout>

            <!-- Refrescar y restaurar. -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="14dp"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <ImageButton
                    android:id="@+id/btn_refresh"
                    android:layout_width="52dp"
                    android:layout_height="44dp"
                    android:padding="10dp"
                    android:scaleType="fitCenter"
                    android:background="@drawable/chip_background"
                    android:src="@drawable/ic_refresh"
                    android:focusable="true"
                    android:clickable="true"
                    android:contentDescription="@null" />

                <ImageButton
                    android:id="@+id/btn_restore"
                    android:layout_width="52dp"
                    android:layout_height="44dp"
                    android:layout_marginStart="8dp"
                    android:padding="10dp"
                    android:scaleType="fitCenter"
                    android:background="@drawable/chip_background"
                    android:src="@drawable/ic_restore"
                    android:focusable="true"
                    android:clickable="true"
                    android:contentDescription="@null" />

                <TextView
                    android:id="@+id/actions_hint"
                    style="@style/AboutHint"
                    android:layout_width="0dp"
                    android:layout_weight="1"
                    android:layout_marginStart="12dp"
                    android:layout_marginTop="0dp" />
            </LinearLayout>
        </LinearLayout>

        <Button android:id="@+id/close_button" style="@style/AboutOutlineButton" android:layout_gravity="end" android:layout_marginTop="4dp" />
    </LinearLayout>
</ScrollView>
